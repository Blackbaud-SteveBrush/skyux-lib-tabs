export interface SkyTabButtonConfig {
  buttonId: string;
  disabled: boolean;
  heading: string;
  headingCount: number;
  isActive: boolean;
  isCloseable: boolean;
  panelId: string;
  tabIndex: number;
}
